// Entry point for groceries JS
import { init as initProductForms } from './product-forms.js';

export function init() {
    initProductForms(); // Explicit init

    // Can then add future groceries/dashboard.js code here
}