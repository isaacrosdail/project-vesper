
export default {
    // Enables using await inside top-level script
    compilerOptions: {
        experimental: {
            async: true
        }
    },
    vitePlugin: {
        inspector: true
    },
};
